const sum = (number1, number2)=> Number(number1) + Number(number2)

const subtration = (number1, number2)=> Number(number1) - Number(number2)

const division = (number1, number2)=> Number(number1) / Number(number2)

const multiplication = (number1, number2)=> Number(number1) * Number(number2)

export const operations = {'+':sum,'-':subtration,'/':division,'*':multiplication}